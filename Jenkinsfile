pipeline {
    agent any 
    stages {
        stage ('Clone')
        {
            steps {
                git 'https://github.com/tbnghiep1110/jenkins-pipeline.git'
            }
        }
    }
}
